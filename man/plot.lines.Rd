\name{plot.lines}
\alias{plot.lines}
\title{plots the path lines (each j, against each r-step)}
\usage{
  plot.lines(xval = NULL, ind, path, l.crit, active = TRUE,
    col = NULL)
}
\description{
  plots the path lines (each j, against each r-step)
}

